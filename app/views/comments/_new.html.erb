<%= form_for ([@idea, @idea.comments.build]) do |f| %>
  <div class="field">
    <%= f.text_field :comment_text , :class => 'textbox', :placeholder => 'Comment here...' %>
  </div>
  <div class="actions">
    <%= f.submit "Post", :class => 'submit' %>
  </div>
<% end %>
